.profile-content
  %h3 Edit the location of your organization
  = simple_form_for(@user) do |f|
    = f.error_notification
    .form-inputs
      .row
        .col-md-6.col-md-offset-3
          %h4.text-center Location
      .row
        .col-md-6.col-md-offset-3
          = f.input :agency_name
          = f.input :web_url
          = f.input :street_address
          = f.input :city
          = f.input :state
          = f.input :zip, label: '5 Digit Zip Code'
      %hr.featurette-divider
        .row
          .col-md-6.col-md-offset-3
            = f.input :email, label: 'Change the login email associated with this account:',
                      hint: 'You will only be able to log into your account with the email above.'
    .text-sm-right
      .form-actions
        .row
          .col-md-6.col-md-offset-3
            = f.button :submit, "Save Details", { class: 'btn btn-success' }
